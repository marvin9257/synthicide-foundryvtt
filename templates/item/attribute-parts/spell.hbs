{{! Legacy Spell Attributes (deprecated) }}
<section
  class='tab attributes {{tab.cssClass}}'
  data-group='primary'
  data-tab='attributes'
>
  <p>This item type is deprecated. Convert it to a <code>trait</code> type
     to access the current attribute layout.</p>
  <div class='resource'>
    <label class='resource-label'>{{localize 'SYNTHICIDE.Item.Trait.FIELDS.level.label'}}</label>
    <input
      type='number'
      name='system.level'
      value='{{system.level}}'
      data-dtype='Number'
      min='0' max='9'
    />
  </div>
</section>