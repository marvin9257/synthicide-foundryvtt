<section class="synthicide-action-roll-card synthicide-item-description-card" data-subtype="{{subtype}}">
  <header class="card-header">
    <h4 class="card-title item-header">{{title}}</h4>
  </header>

  {{#if flavor}}
    <div class="notes">{{{flavor}}}</div>
  {{/if}}
</section>
